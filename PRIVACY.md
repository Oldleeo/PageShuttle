# 页梭隐私说明

最后更新：2026-09-01

页梭由 [老李Oldlee](https://x.com/oldleeoo) 开发。页梭的目标是在不修改 Windows 或 macOS 系统代理的前提下，为当前 Chrome 用户配置提供独立代理和网页环境一致性功能。

## 本地保存的数据

以下数据保存在 Chrome 的 `chrome.storage.local` 中：

- 用户导入的代理节点、服务器地址、端口和认证信息；
- 收藏分组、选中节点和代理范围；
- 手动设置的经纬度、国家代码、网页语言和时区；
- 最近一次更新检查结果。

页梭没有作者运营的账号系统、统计服务器或广告系统，不会把这些数据上传给作者。

## 必要的网络请求

- 用户启动代理或点击检测出口 IP 时，页梭会向 `https://ipwho.is/` 请求出口 IP、国家、城市级坐标和时区。该服务会看到请求的出口 IP。
- 用户导入订阅地址时，Chrome 会直接访问用户填写的 URL；订阅内容只在本机解析和保存。
- 检查更新时，本地助手会访问 `github.com` 和 `githubusercontent.com`，获取公开的版本清单和签名发行包。
- 用户访问网页产生的网络流量会按照用户选择的代理节点发送给对应代理服务商。节点服务商的数据处理规则不由页梭控制。

## 本地助手

本地助手通过 Chrome Native Messaging 与扩展通信，只在 `127.0.0.1` 启动临时 SOCKS5 端口。它不安装系统级后台服务，不创建 TUN/TAP 设备，不修改 Windows、macOS、WinHTTP、环境变量或防火墙代理配置。

## 网页环境保护

启用浏览器环境功能后，页梭会在网页中调整网站可读取的地理定位、JavaScript locale、时区、本地时间和部分字体探测结果。该功能不修改操作系统的时间、语言、定位或字体，也不保证对所有指纹技术完全隐身。

## 删除数据

在 Chrome 中移除扩展会删除该 Chrome 配置中的扩展存储。运行发行包中的 Windows 或 macOS 卸载脚本会删除本地助手及其运行文件。用户导入的节点不会同步到其他设备。

## 联系方式

问题或建议请通过 [GitHub Issues](https://github.com/Oldleeo/PageShuttle/issues) 或 [X：@oldleeoo](https://x.com/oldleeoo) 联系。
